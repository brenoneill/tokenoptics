<!--
  Claude Code reads this file automatically on every session.
  The agent rules live in AGENTS.md (cross-tool convention) and are
  imported below so they apply to Claude Code too. Don't duplicate
  rules here — add them to AGENTS.md so every agent sees them.
-->

@AGENTS.md
